import unittest, random, sys, time, os
sys.path.extend(['.', '..', '../..', 'py'])
import h2o, h2o_cmd, h2o_browse as h2b, h2o_import as h2i, h2o_exec as h2e
zeroList = ['Result0 = 0', 'Result.hex = 0']
exprList = ['Result<n> = max(<keyX>[,<col1>])']
if (__name__ == '__main__'):
    h2o.unit_main()
