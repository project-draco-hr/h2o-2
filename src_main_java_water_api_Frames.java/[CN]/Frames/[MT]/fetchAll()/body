{
  Set<Key> keySet=H2O.globalKeySet("water.fvec.Frame");
  Map<String,Frame> framesMap=new TreeMap();
  for (  Key key : keySet) {
    if (!key.user_allowed())     continue;
    if (H2O.get(key) == null)     continue;
    String keyString=key.toString();
    Value value=DKV.get(key);
    Iced pojo=value.get();
    if (!(pojo instanceof Frame))     continue;
    Frame frame=(Frame)pojo;
    framesMap.put(keyString,frame);
  }
  return framesMap;
}
