{
  Key[] ks=new Key[25];
  int i=0;
  for (  Key key : H2O.keySet())   if (key.user_allowed()) {
    ks[i++]=key;
    if (i >= ks.length)     break;
  }
  _keys=i < ks.length ? Arrays.copyOf(ks,i) : ks;
  return this;
}
