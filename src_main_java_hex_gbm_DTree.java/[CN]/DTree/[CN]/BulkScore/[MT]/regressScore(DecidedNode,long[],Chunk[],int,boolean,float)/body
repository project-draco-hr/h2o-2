{
  int bin=prev.bin(chks,i);
  float[] ycls=prev._ycls[bin];
  float num=ycls[0];
  clss[0]+=num;
  sum+=prev._pred[bin] * num;
  if (num == 0) {
    prev=prev._tree.decided(prev._pid);
    return regressScore(prev,clss,chks,i,false,sum);
  }
  return sum;
}
