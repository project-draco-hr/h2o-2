{
  super(key,dataKey,fr);
  this.N=ntrees;
  this.errs=errs;
  this.ymin=ymin;
  this.cm=cm;
  treeBits=new CompressedTree[forest.length];
  for (int i=0; i < forest.length; i++)   treeBits[i]=forest[i].compress();
}
