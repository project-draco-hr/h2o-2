{
  long cms[][][]=this.cms;
  if (cms != null && cms.length > 0) {
    int n=cms.length - 1;
    while (n > 0 && cms[n] == null)     --n;
    return cms[n] == null ? null : new ConfusionMatrix(cms[n]);
  }
 else   return null;
}
