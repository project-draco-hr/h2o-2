{
  _sb.p(" (Double.isNaN(data[").p(col).p("]) ||");
  boolean first=true;
  for (int i=0; i < gcmp._val.length; i++) {
    if (gcmp._val[i] == 0)     continue;
    for (int j=0; j < 8; j++) {
      if ((gcmp._val[i] & (1 << j)) == 1) {
        if (first)         _sb.p(" (int) data[").p(col).p(" /* ").p(_tm._names[col]).p(" */").p("] !=").p((i << 3) + j);
 else         _sb.p(" && (int) data[").p(col).p("] !=").p((i << 3) + j);
        first=false;
      }
    }
  }
}
