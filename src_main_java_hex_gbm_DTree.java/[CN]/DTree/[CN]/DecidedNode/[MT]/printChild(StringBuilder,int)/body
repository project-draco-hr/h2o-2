{
  for (int i=0; i < _nids.length; i++)   if (_nids[i] == nid)   return sb.append("[").append(_bmin + i * _step).append(" <= ").append(_tree._names[_col]).append(" < ").append(_bmin + (i + 1) * _step).append("]");
  throw H2O.fail();
}
