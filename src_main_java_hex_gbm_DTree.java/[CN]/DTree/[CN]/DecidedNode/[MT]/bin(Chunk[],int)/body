{
  if (_nids.length == 1)   return 0;
  if (chks[_col].isNA0(i))   return i % _nids.length;
  float d=(float)chks[_col].at0(i);
  int idx1=(int)((d - _bmin) / _step);
  int bin=Math.max(Math.min(idx1,_nids.length - 1),0);
  return bin;
}
