{
  if (_col == -1)   return "Decided has col = -1";
  String n=" <= " + _tree._names[_col] + " < ";
  String s=new String();
  float f=_bmin;
  for (int i=0; i < _nids.length; i++)   s+=f + n + (f+=_step)+ " = "+ Arrays.toString(_pred[i])+ "\n";
  return s;
}
