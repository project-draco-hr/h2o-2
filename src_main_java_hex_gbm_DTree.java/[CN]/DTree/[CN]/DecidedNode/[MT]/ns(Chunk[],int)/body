{
  if (_split._col == -1) {
    System.out.println(Arrays.toString(_nids));
    for (int i=0; i < _nids.length; i++)     if (_nids[i] != -1)     System.out.println("i=" + i + ", nid="+ _nids[i]+ " "+ _tree.node(_nids[i]));
    return 0;
  }
  return _nids[bin(chks,row)];
}
