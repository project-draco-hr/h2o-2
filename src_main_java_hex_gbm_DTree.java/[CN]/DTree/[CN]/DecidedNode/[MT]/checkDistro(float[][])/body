{
  for (  float fs[] : fss) {
    if (fs == null)     continue;
    float sum=0;
    for (    float f : fs)     sum+=f;
    if (!(-0.00001 < sum && sum < 0.00001))     return false;
  }
  return true;
}
