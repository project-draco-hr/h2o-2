{
synchronized (Weaver.class) {
    String[] a=_packages;
    if (ArrayUtils.indexOf(a,name) < 0) {
      String[] t=Arrays.copyOf(a,a.length + 1);
      t[t.length - 1]=name;
      _packages=t;
    }
  }
}
