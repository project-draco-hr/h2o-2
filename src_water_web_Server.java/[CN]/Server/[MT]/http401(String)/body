{
  RString r=new RString(Page.html);
  r.replace("contents","You are not authorized to view " + uri + " try to <a href=\"loginQuery\">login</a> first.");
  return new Response(NanoHTTPD.HTTP_OK,NanoHTTPD.MIME_HTML,r.toString());
}
