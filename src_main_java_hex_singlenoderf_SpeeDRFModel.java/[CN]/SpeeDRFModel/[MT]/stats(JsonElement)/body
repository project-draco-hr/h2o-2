{
  if (json == null) {
    return null;
  }
 else {
    JsonObject obj=json.getAsJsonObject();
    return new double[]{obj.get(Constants.MIN).getAsDouble(),obj.get(Constants.MAX).getAsDouble(),obj.get(Constants.MEAN).getAsDouble()};
  }
}
