{
  randomizeWeights();
  for (int i=0; i <= parameters.hidden.length + 1; ++i) {
    if (parameters.activation == NN.Activation.Rectifier || parameters.activation == NN.Activation.Maxout) {
      Arrays.fill(biases[i],1.);
    }
  }
}
