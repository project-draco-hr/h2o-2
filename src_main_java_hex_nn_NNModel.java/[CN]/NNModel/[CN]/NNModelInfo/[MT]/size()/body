{
  long[] siz=new long[2];
  for (  float[] w : weights) {
    siz[0]+=w.length;
    siz[1]+=w.length * Float.SIZE;
  }
  for (  double[] b : biases) {
    siz[0]+=b.length;
    siz[1]+=b.length * Double.SIZE;
  }
  siz[1]/=8;
  return siz;
}
