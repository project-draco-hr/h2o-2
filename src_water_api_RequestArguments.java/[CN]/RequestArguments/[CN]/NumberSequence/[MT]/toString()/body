{
  if (_str != null)   return _str;
  if (_arr == null || _arr.length == 0)   return "";
  StringBuilder res=new StringBuilder();
  res.append(_arr[0]);
  for (int i=1; i < _arr.length; ++i)   res.append("," + _arr[i]);
  return res.toString();
}
