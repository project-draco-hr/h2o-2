{
  int classCol=_classCol.value();
  ValueArray va=_key.value();
  List<Integer> res=Lists.newArrayList();
  for (int i=0; i < va._cols.length; i++)   if (va._cols[i]._min != va._cols[i]._max && i != classCol)   res.add(i);
  return Ints.toArray(res);
}
