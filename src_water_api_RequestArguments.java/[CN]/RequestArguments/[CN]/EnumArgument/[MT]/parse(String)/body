{
  for (  T v : _enumClass.getEnumConstants())   if (v.toString().equals(input))   return v;
  throw new IllegalArgumentException("Only " + Arrays.toString(selectValues()) + " accepted for argument "+ _name);
}
