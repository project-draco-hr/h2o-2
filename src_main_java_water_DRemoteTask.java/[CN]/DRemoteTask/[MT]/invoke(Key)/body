{
  try {
    dfork(keys).get();
  }
 catch (  ExecutionException eex) {
    Log.errRTExcept(eex);
  }
catch (  InterruptedException iex) {
    Log.errRTExcept(iex);
  }
catch (  CancellationException cex) {
    Log.errRTExcept(cex);
  }
  if (_exception != null)   throw _exception.toEx();
  return self();
}
