{
  if (_is_local) {
    try {
      lcompute();
    }
 catch (    RuntimeException e) {
      _exception=new DException(e);
      completeExceptionally(e);
    }
catch (    AssertionError e) {
      _exception=new DException(e);
      completeExceptionally(e);
    }
  }
 else   dcompute();
}
