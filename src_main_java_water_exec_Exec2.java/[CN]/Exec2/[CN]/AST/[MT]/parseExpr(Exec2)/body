{
  AST ast;
  if (E.peek('(')) {
    int x=E._x;
    ast=parseCXExpr(E);
    if (!E.peek(')'))     E.throwErr("Missing close-paren",x);
    return ast;
  }
  ast=ASTKey.parse(E);
  if (ast != null)   return ast;
  ast=ASTNum.parse(E);
  if (ast != null)   return ast;
  return null;
}
