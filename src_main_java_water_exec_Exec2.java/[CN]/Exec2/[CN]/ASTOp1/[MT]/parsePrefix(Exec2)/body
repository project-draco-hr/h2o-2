{
  int x=E._x;
  String id=E.isID();
  if (id == null)   return null;
  ASTOp1 op1=OP1S.get(id);
  if (op1 == null) {
    E._x=x;
    return null;
  }
  E.xpeek('(',x,null);
  return E.xpeek(')',E._x,op1.make(parseCXExpr(E)));
}
