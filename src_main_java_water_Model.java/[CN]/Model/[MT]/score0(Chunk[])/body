{
  double tmp[]=new double[_names.length];
  float preds[]=new float[nclasses()];
  int len=chks[0]._len;
  for (int i=0; i < len; i++)   chks[_names.length].set0(i,Utils.maxIndex(score0(chks,i,tmp,preds)));
}
