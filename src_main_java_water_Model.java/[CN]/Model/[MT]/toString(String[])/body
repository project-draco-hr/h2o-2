{
  SB sb=new SB();
  sb.p('{');
  for (int i=0; i < ss.length - 1; i++)   sb.p('"').p(ss[i]).p("\",");
  if (ss.length > 0)   sb.p('"').p(ss[ss.length - 1]).p('"');
  sb.p('}');
  return sb.toString();
}
