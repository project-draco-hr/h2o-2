{
  Vec resp=fr._vecs[fr._vecs.length - 1];
  String[] domain=resp._domain;
  if (resp._domain == null) {
    int min=(int)resp.min();
    int max=(int)resp.max();
    domain=new String[max - min + 1];
    for (int i=0; i < domain.length; i++)     domain[i]="" + (min + i);
  }
  return domain;
}
