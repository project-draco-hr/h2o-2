import unittest
import random, sys, time, os
sys.path.extend(['.', '..', 'py'])
import h2o, h2o_cmd, h2o_hosts, h2o_import as h2i, h2o_exec as h2e
import codecs
print 'This test makes sure python creates a utf16 file, that is not a ascii file'
print "apparently need to have at least one normal character otherwise the parse doesn't work right"
UTF16 = True
UTF8 = False
UTF8_MULTIBYTE = False
if UTF8:
    nonQuoteChoices = range(0, 256)
else:
    nonQuoteChoices = range(0, 128)
if UTF8_MULTIBYTE:
    nonQuoteChoices = range(0, 64)
    nonQuoteChoices += [8220, 8221, 8216, 8217, 6000]
nonQuoteChoices.remove(9)
nonQuoteChoices.remove(0)
nonQuoteChoices.remove(13)
nonQuoteChoices.remove(10)
nonQuoteChoices.remove(1)
nonQuoteChoices.remove(59)
nonQuoteChoices.remove(32)
nonQuoteChoices.remove(34)
nonQuoteChoices.remove(44)
nonQuoteChoices.remove(48)
nonQuoteChoices.remove(49)
nonQuoteChoices.remove(50)
nonQuoteChoices.remove(51)
nonQuoteChoices.remove(52)
nonQuoteChoices.remove(53)
nonQuoteChoices.remove(54)
nonQuoteChoices.remove(55)
nonQuoteChoices.remove(56)
nonQuoteChoices.remove(57)
if (__name__ == '__main__'):
    h2o.unit_main()
