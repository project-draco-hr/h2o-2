{
  if (!p.containsKey(name))   return defaultValue;
  try {
    return Integer.parseInt(p.getProperty(name));
  }
 catch (  NumberFormatException e) {
    throw new GLMInputException("invalid value of argument " + name);
  }
}
