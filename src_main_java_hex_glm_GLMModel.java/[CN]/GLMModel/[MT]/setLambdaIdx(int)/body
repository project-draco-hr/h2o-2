{
  best_lambda_idx=l;
  threshold=submodels[l].validation.best_threshold;
  if (beta == null)   beta=MemoryManager.malloc8d(this.coefficients_names.length);
 else   Arrays.fill(beta,0);
  int j=0;
  for (  int i : submodels[l].idxs)   beta[i]=submodels[l].beta[j++];
}
