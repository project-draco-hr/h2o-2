{
  Key[] xvals=m.xvalModels();
  if (xvals != null) {
    H2OCountedCompleter t=getCurrentTask();
    t.addToPendingCount(xvals.length);
    for (int i=0; i < xvals.length; ++i)     new UnlockModelTask(t,xvals[i],_jobKey).forkTask();
  }
  m.unlock(_jobKey);
}
