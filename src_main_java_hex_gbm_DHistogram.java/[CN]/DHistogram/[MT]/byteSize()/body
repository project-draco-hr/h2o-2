{
  long sum=8 + 8;
  sum+=1 + 2;
  sum+=4 + 4 + 4;
  sum+=8 * 3;
  if (_bins == null)   return sum;
  sum+=24 + _bins.length << 3;
  sum+=20 + _mins.length << 2;
  sum+=20 + _maxs.length << 2;
  sum+=byteSize0();
  return sum;
}
