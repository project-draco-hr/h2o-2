{
  int sum=14 * 8;
  sum+=byteSize(_bins);
  sum+=byteSize(_mins);
  sum+=byteSize(_maxs);
  sum+=byteSize(_Ms);
  sum+=byteSize(_Ss);
  sum+=byteSize(_MSEs);
  if (_clss != null) {
    sum+=(_clss.length + 3) * 8;
    for (    long[] ls : _clss)     sum+=byteSize(ls);
  }
  return sum;
}
