{
  int x=_nbin - 1;
  while (_bins[x] == 0)   x--;
  if (DRF._optflags == 0)   return _maxs[x];
  if (x == _nbin - 1)   return _max;
  float max=binAt(x + 1);
  if (_isInt > 0)   max=(float)Math.floor(max);
  return max;
}
