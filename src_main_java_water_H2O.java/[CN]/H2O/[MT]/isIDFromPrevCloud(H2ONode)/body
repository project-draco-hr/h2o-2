{
  if (h2o == null)   return false;
  HeartBeat hb=h2o._heartbeat;
  int hash=hb._cloud_hash;
  for (int i=0; i < 256; i++)   if (CLOUDS[i] != null && hash == CLOUDS[i]._hash)   return true;
  return false;
}
