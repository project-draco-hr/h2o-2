{
  Random rand=new Random(SEED);
  for (int r=0; r < array[0].length; r++) {
    final int goal=rand.nextInt(goals.length);
    for (int c=0; c < array.length; c++)     array[c][r]=goals[goal][c] + rand.nextGaussian();
  }
}
