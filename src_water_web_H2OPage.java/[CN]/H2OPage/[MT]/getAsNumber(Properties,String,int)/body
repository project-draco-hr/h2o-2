{
  int result=def;
  try {
    String s=args.getProperty(arg,"");
    if (!s.isEmpty())     result=Integer.parseInt(s);
  }
 catch (  NumberFormatException e) {
    return def;
  }
  return result;
}
