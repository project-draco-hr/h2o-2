{
  if (_a == null || _a.length != dinfo.fullN())   _a=new double[dinfo.fullN()];
  Arrays.fill(_a,0.);
  if (nums.length > 0)   System.arraycopy(nums,0,_a,0,nums.length);
  for (  int cat : cats) {
    assert(cat > nums.length);
    _a[cat]=1.0;
  }
}
