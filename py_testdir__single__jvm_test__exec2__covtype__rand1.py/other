import unittest, random, sys, time
sys.path.extend(['.', '..', 'py'])
import h2o, h2o_cmd, h2o_browse as h2b, h2o_exec as h2e, h2o_hosts, h2o_import as h2i
zeroList = ['Result.hex = Result0 = c(0)', 'Result.hex = Result = c(0)']
exprList = ['Result.hex = Result<n> = c.hex[,<col1>] = ((c.hex[,2]==0) ? 54321 : 54321))', 'Result.hex = Result<n> = c.hex[,<col1>]', 'Result.hex = Result<n> = min(c.hex[,<col1>], c.hex[,<col1>])', 'Result.hex = Result<n> = max(c.hex[,<col1>], c.hex[,<col1>]) + Result.hex[,0]', 'Result.hex = Result<n> = sum(c.hex[,<col1>], c.hex[,<col1>]) + Result.hex[,0]']
if (__name__ == '__main__'):
    h2o.unit_main()
