{
  int hrs=(int)(t / (60 * 60 * 1000));
  t-=hrs * 60 * 60* 1000;
  int mins=(int)(t / 60000);
  t-=mins * 60000;
  int secs=(int)(t / 1000);
  t-=secs * 1000;
  int millis=(int)t;
  return hrs + "hrs " + mins+ "m "+ secs+ "s "+ millis+ "ms";
}
