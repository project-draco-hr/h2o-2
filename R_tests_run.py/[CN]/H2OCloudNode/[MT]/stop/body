def stop(self):
    if (self.pid > 0):
        print 'Killing JVM with PID {}'.format(self.pid)
        try:
            child = self.child
            child.terminate()
        except OSError:
            pass
        self.pid = (-1)
