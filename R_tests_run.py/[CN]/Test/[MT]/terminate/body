def terminate(self):
    self.terminated = True
    if (self.pid > 0):
        print 'Killing Test with PID {}'.format(self.pid)
        try:
            child = self.child
            child.terminate()
        except OSError:
            pass
    self.pid = (-1)
