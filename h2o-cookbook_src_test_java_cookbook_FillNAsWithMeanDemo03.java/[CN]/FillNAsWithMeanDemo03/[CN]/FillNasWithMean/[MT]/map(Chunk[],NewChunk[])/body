{
  for (int j=0; j < xs.length; j++) {
    for (int l=0; l < xs[j]._len; l++) {
      if (xs[j].isNA0(l)) {
        ns[j].addNum(_meanX[j]);
      }
 else {
        ns[j].addNum(xs[j].at0(l));
      }
    }
  }
}
