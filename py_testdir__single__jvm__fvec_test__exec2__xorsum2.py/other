import unittest, random, sys, time
sys.path.extend(['.', '..', 'py'])
import h2o, h2o_browse as h2b, h2o_exec as h2e, h2o_hosts, h2o_import as h2i, h2o_cmd, h2o_util
exprList = ['a=c(1); a = sum(r1[,1])', 'h=c(1); h = xorsum(r1[,1])']
if (__name__ == '__main__'):
    h2o.unit_main()
