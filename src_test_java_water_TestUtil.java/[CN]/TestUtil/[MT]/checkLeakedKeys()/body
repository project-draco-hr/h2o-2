{
  DKV.get(Job.LIST);
  DKV.write_barrier();
  int leaked_keys=H2O.store_size() - _initial_keycnt;
  if (leaked_keys != 0)   for (  Key k : H2O.keySet())   System.err.println("Leaked key: " + k);
  assertEquals("No keys leaked",0,leaked_keys);
}
