{
  if (len > 65536) {
    System.out.println("len=" + len + " nz="+ nzcnt);
    throw H2O.unimpl();
  }
  byte[] buf=new byte[nzcnt * 2 + OFF];
  UDP.set4(buf,0,len);
  int j=OFF;
  for (int i=0; i < len; i++)   if (ls[i] != 0)   j+=UDP.set2(buf,j,(short)i);
  return buf;
}
