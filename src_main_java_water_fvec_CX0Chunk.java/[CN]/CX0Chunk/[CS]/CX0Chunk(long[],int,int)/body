{
  _start=-1;
  _len=len2;
  _last=OFF;
  byte[] buf=new byte[nzcnt * 2 + OFF];
  UDP.set4(buf,0,len2);
  int j=OFF;
  if (len2 > 65535)   throw H2O.unimpl();
  for (int i=0; i < len2; i++)   if (ls[i] != 0)   j+=UDP.set2(buf,j,(short)i);
  _mem=buf;
}
