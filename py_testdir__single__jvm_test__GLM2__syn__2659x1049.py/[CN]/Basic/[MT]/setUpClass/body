@classmethod
def setUpClass(cls):
    global localhost
    h2o.beta_features = True
    localhost = h2o.decide_if_localhost()
    if localhost:
        h2o.build_cloud(1)
    else:
        h2o_hosts.build_cloud_with_hosts(1)
