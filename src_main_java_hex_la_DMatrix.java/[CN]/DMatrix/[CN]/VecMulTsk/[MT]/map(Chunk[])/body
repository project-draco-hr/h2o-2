{
  _res=MemoryManager.malloc8d(chks.length - 1);
  for (int i=0; i < chks[0]._len; i=chks[0].nextNZ(i)) {
    final double y=chks[0].at0(i);
    for (int j=1; j < chks.length; ++j)     _res[j - 1]+=y * chks[j].at0(i);
  }
}
