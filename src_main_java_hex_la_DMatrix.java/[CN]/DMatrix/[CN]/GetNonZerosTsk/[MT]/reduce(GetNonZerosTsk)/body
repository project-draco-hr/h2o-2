{
  if (_res.length + gnz._res.length > _maxsz)   throw new RuntimeException("too many nonzeros! found at least " + (_res.length + gnz._res.length > _maxsz) + " nonzeros.");
  _res=Utils.mergeSort(_res,gnz._res);
}
