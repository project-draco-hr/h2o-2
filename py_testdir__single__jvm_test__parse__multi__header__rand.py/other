import unittest, time, sys, random
sys.path.extend(['.', '..', 'py'])
import h2o, h2o_cmd, h2o_hosts
import h2o_browse as h2b
paramsDict = {'parser_type': [None, 'AUTO'], 'separator': [None, ',', '\t', ' '], 'header': [None, 0, 1], 'header_from_file': [None, 'syn_header', 'syn_data'], }
if (__name__ == '__main__'):
    h2o.unit_main()
