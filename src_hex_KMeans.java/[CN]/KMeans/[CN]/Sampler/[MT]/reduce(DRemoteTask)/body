{
  if (DEBUG)   System.out.println("sampler reduce " + this);
  Sampler task=(Sampler)rt;
  if (_newClusters != null)   _newClusters=merge(_newClusters,task._newClusters);
 else   _newClusters=task._newClusters;
}
