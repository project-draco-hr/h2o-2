def test_exec_import_hosts(self):
    importFolderPath = '/home/0xdiag/datasets'
    if localhost:
        maxTrials = 200
        csvFilenameAll = [('covtype.data', 'cA', 15), ('covtype.data', 'cB', 15)]
    else:
        maxTrials = 20
        csvFilenameAll = [('covtype.data', 'cA', 15), ('covtype20x.data', 'cC', 60)]
    csvFilenameList = csvFilenameAll
    h2b.browseTheCloud()
    lenNodes = len(h2o.nodes)
    for (csvFilename, key2, timeoutSecs) in csvFilenameList:
        h2i.setupImportFolder(None, importFolderPath)
        parseKey = h2i.parseImportFolderFile(None, csvFilename, importFolderPath, key2=key2, timeoutSecs=2000)
        print csvFilename, 'parse time:', parseKey['response']['time']
        print "Parse result['Key']:", parseKey['destination_key']
        inspect = h2o_cmd.runInspect(None, parseKey['destination_key'])
        print ('\n' + csvFilename)
        h2e.exec_zero_list(zeroList)
        h2e.exec_expr_list_rand(lenNodes, exprList, key2, maxCol=54, maxRow=400000, maxTrials=maxTrials, timeoutSecs=timeoutSecs)
