{
  _x=new int[256];
  C1NChunk c1n=(C1NChunk)chk;
  byte[] bits=c1n._mem;
  for (  byte b : bits)   _x[b & 0xFF]++;
  if (H2O.SELF.toString().equals(_throwAt))   throw new RuntimeException("test");
}
