import unittest, time, sys
sys.path.extend(['.', '..', '../..', 'py'])
import h2o, h2o_cmd, h2o_glm, h2o_import as h2i
import h2o_browse as h2b
argcaseList = [{'response': 106, 'family': 'gaussian', 'lambda': 1e-05, 'max_iter': 50, 'n_folds': 0, 'alpha': 1, 'beta_epsilon': 0.0001, }]
if (__name__ == '__main__'):
    h2o.unit_main()
