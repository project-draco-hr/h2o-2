import unittest, random, sys, time
sys.path.extend(['.', '..', 'py'])
import h2o, h2o_cmd, h2o_hosts, h2o_browse as h2b, h2o_import2 as h2i, h2o_exec as h2e
zeroList = ['Result2 = 0', 'Result1 = 0', 'Result0 = 0', 'Result.hex = 0']
exprList = ['Result<n> = <keyX>[<col1>] + <keyX>[<col2>] + <keyX>[2]', 'Result<n> = randomFilter(<keyX>,<col1>,<row>)']
if (__name__ == '__main__'):
    h2o.unit_main()
