{
  int res=0;
  if (_beta == null)   return res;
  for (int i=0; i < _beta.length - 1; ++i)   if (_beta[i] != 0)   ++res;
  System.out.println("rank = " + res + ", beta = "+ Arrays.toString(_beta));
  return res;
}
