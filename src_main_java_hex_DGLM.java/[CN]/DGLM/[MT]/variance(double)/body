{
switch (this) {
case gaussian:
    return 1;
case binomial:
  if (0 <= mu && mu <= 1)   return mu * (1 - mu);
return 1;
case poisson:
return mu;
case gamma:
return mu * mu;
default :
throw new RuntimeException("unknown family Id " + this);
}
}
