{
  _xy=MemoryManager.malloc8d(N);
  _xx=new double[N - diag][];
  _diag=MemoryManager.malloc8d(diag);
  int dense=N - _diag.length;
  for (int i=0; i < dense; ++i)   _xx[i]=MemoryManager.malloc8d(diag + i + 1);
}
