def rand_rowData(colCount):
    rowData = str(random.uniform(0, colCount))
    for i in range(colCount):
        rowData = ((rowData + ',') + str(random.uniform((-100000.0), 100000.0)))
    return rowData
