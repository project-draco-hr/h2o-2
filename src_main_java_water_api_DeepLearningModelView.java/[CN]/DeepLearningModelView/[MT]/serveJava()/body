{
  deeplearning_model=UKV.get(_modelKey);
  if (deeplearning_model != null && !deeplearning_model.get_params().autoencoder && deeplearning_model.get_params().activation != DeepLearning.Activation.Maxout)   return deeplearning_model.toJava();
 else   return "";
}
