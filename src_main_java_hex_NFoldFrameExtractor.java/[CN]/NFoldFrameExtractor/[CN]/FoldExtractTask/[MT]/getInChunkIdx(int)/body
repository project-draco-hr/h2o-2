{
  if (_inFold)   return _startFoldChkIdx == _startRestChkIdx ? _startFoldChkIdx : coutidx + _startFoldChkIdx;
 else {
    if (coutidx < _precedingChks)     return coutidx;
 else     return _startRestChkIdx + (coutidx - _precedingChks);
  }
}
