{
  double sum=0;
  int numClasses=_clss[0].length;
  for (int i=0; i < _bins.length; i++) {
    if (_bins[i] == 0)     continue;
    int err=0;
    for (int j=0; j < numClasses; j++)     err+=_clss[i][j] * _clss[i][j];
    sum+=(double)_bins[i] - ((double)err / _bins[i]);
  }
  return sum;
}
