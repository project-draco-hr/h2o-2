{
  double sum=0;
  double sum2=0;
  int nbins=_bins.length;
  for (int i=0; i < nbins; i++) {
    double m=mean(i);
    double x=m == 0.0 ? 0 : var(i) / m;
    sum+=x;
    sum2+=mse(i) * _bins[i];
  }
  return sum2;
}
