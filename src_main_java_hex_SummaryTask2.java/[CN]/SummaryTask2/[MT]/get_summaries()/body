{
  if (_summaries == null) {
    _summaries=new Summary2[_fr.vecs().length];
    for (int i=0; i < _summaries.length; i++)     _summaries[i]=new Summary2(_fr.vecs()[i],_fr.names()[i],null);
    doAll(_fr);
  }
  return _summaries;
}
