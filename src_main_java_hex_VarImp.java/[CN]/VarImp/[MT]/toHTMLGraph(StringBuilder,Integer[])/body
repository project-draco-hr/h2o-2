{
  Integer[] so=varimp.length > max_var ? sortOrder : null;
  DocGen.HTML.graph(sb,"graphvarimp","g_varimp",DocGen.HTML.toJSArray(new StringBuilder(),variables,so,Math.min(max_var,variables.length)),DocGen.HTML.toJSArray(new StringBuilder(),varimp,so,Math.min(max_var,variables.length)));
  return sb;
}
