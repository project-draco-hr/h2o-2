{
  int readLen=readInternal(b,off,len);
  if (readLen != -1) {
    int pos=findBoundary(b,off,readLen);
    if (pos != -1) {
      while (wrappedIs.read() != -1)       ;
      return pos - off;
    }
  }
  return readLen;
}
