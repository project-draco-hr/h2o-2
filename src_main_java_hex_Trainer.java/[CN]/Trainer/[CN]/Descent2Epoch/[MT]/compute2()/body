{
  if (_count < 0 || --_count > 0) {
    for (    Chunk[] cs : _node._chunks) {
      Descent2Chunk task=new Descent2Chunk();
      task._node=_node;
      task._cs=cs;
      H2O.submitTask(task);
    }
    reinitialize();
    H2O.submitTask(this);
  }
}
