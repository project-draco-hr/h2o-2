{
  int b=bin(col_data);
  AtomicLongArray.incr(_bins,b);
  AtomicFloatArray.setMin(_mins,b,col_data);
  AtomicFloatArray.setMax(_maxs,b,col_data);
  if (y != 0) {
    AtomicDoubleArray.add(_sums,b,y);
    AtomicDoubleArray.add(_ssqs,b,y * y);
  }
}
