{
  DBinHistogram hists[]=new DBinHistogram[ncols];
  Vec[] vs=fr._vecs;
  for (int j=0; j < ncols; j++) {
    Vec v=vs[j];
    hists[j]=v.min() == v.max() ? null : new DBinHistogram(fr._names[j],nclass,v._isInt,(float)v.min(),(float)v.max(),v.length());
  }
  return hists;
}
