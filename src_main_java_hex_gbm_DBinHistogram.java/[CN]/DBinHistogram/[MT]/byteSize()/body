{
  long sum=super.byteSize();
  sum+=8 + byteSize(_bins);
  sum+=8 + byteSize(_mins);
  sum+=8 + byteSize(_maxs);
  sum+=8 + byteSize(_Ms);
  sum+=8 + byteSize(_Ss);
  sum+=8 + byteSize(_MSEs);
  sum+=8 + byteSize(_clss);
  if (_clss != null)   for (int i=0; i < _clss.length; i++)   sum+=byteSize(_clss[i]);
  return sum;
}
