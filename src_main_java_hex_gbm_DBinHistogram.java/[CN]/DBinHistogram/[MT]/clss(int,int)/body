{
  byte bs[]=_clss1[bin];
  long x=bs == null ? 0 : bs[cls];
  if (_clss2 == null)   return x;
  short ss[]=_clss2[bin];
  x+=(ss == null ? 0 : ss[cls]);
  if (_clss4 == null)   return x;
  int is[]=_clss4[bin];
  x+=(is == null ? 0 : is[cls]);
  if (_clss8 == null)   return x;
  long ls[]=_clss8[bin];
  return x + (ls == null ? 0 : ls[cls]);
}
