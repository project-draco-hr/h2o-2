{
  if (this == nv)   return;
  long e1[]=nv._espc;
  if (e1.length > _espc.length) {
    e1=_espc;
    _espc=nv._espc;
  }
  for (int i=0; i < e1.length; i++)   if (e1[i] != 0 && _espc[i] == 0)   _espc[i]=e1[i];
}
