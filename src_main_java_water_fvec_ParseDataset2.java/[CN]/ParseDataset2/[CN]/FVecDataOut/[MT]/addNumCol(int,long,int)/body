{
  if (colIdx >= _nvs.length)   System.err.println("Additional column (" + _nvs.length + " < "+ colIdx+ ":"+ number+ ","+ exp+ ") on line "+ linenum());
 else   _nvs[_col=colIdx].addNum(number,exp);
}
