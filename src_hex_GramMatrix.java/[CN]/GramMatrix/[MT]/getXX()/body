{
  Matrix xx=new Matrix(_xx.length,_xx.length);
  for (int i=0; i < _n; ++i) {
    for (int j=0; j < _xx[i].length; ++j) {
      xx.set(i,j,_xx[i][j]);
      xx.set(j,i,_xx[i][j]);
    }
  }
  return xx;
}
