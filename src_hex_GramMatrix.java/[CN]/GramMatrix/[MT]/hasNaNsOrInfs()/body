{
  for (int i=0; i < _xx.length; ++i) {
    if (Double.isNaN(_xy[i]) || Double.isInfinite(_xy[i]))     return true;
    for (    double d : _xx[i])     if (Double.isNaN(d) || Double.isInfinite(d))     return true;
  }
  return false;
}
