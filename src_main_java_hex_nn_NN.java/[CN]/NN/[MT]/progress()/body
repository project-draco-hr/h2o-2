{
  if (DKV.get(dest()) == null)   return 0;
  NNModel m=UKV.get(dest());
  if (m != null && m.model_info() != null)   return (float)(m.epoch_counter / m.model_info().get_params().epochs);
  return 0;
}
