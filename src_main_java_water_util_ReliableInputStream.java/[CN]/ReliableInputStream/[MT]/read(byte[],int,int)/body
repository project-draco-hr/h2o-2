{
  int attempts=0;
  while (true) {
    try {
      int res=_is.read(b,off,len);
      ;
      if (res > 0)       _off+=res;
      return res;
    }
 catch (    IOException e) {
      try2Recover(attempts++,e);
    }
  }
}
