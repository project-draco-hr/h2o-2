{
  int attempts=0;
  while (true) {
    try {
      int res=_is.read();
      if (res != -1)       _off+=1;
      return res;
    }
 catch (    IOException e) {
      try2Recover(attempts++,e);
    }
  }
}
