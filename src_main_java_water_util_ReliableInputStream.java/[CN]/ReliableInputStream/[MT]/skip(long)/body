{
  int attempts=0;
  while (true) {
    try {
      long res=_is.skip(n);
      if (res > 0)       _off+=res;
      return res;
    }
 catch (    IOException e) {
      try2Recover(attempts++,e);
    }
  }
}
