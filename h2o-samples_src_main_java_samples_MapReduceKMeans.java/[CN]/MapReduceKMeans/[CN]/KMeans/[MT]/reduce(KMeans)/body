{
  for (int cluster=0; cluster < counts.length; cluster++) {
    for (int column=0; column < sums[0].length; column++)     sums[cluster][column]+=task.sums[cluster][column];
    counts[cluster]+=task.counts[cluster];
  }
  error+=task.error;
}
