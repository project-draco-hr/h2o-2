import unittest, random, sys, time
sys.path.extend(['.', '..', '../..', 'py'])
import h2o, h2o_cmd, h2o_browse as h2b, h2o_import as h2i, h2o_exec as h2e
exprList = ['a=runif(c.hex[,1], -1); rTrain=<keyX>[a<0.8,]']
if (__name__ == '__main__'):
    h2o.unit_main()
