{
  _modelKey.refreshOnChange();
  if (_modelKey.value() != null && _dataKey.value() != null)   _dest=new H2OKey(DEST_KEY,_modelKey.value()._selfKey.toString() + " on " + _dataKey.value()._key);
 else   _dest=new H2OKey(DEST_KEY,true);
}
