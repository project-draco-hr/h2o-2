{
  HashMap<String,String> map=new HashMap<String,String>();
  String[] maps=replace.split(";");
  for (  String s : maps) {
    String[] pair=s.split(":");
    String key=pair[0];
    String value=pair[1];
    map.put(key,value);
  }
  return map;
}
