{
  if (!env.isAry(-2) || !env.isAry(-1))   throw new IllegalArgumentException("Operation requires two frames.");
  Matrix fr1=new Matrix(env.ary(-2));
  Frame out=fr1.mult(env.ary(-1));
  env.push(out);
}
