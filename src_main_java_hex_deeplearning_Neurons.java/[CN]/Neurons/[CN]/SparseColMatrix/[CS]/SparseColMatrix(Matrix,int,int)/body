{
  _rows=rows;
  _cols=new TreeMap[cols];
  for (int j=0; j < cols; ++j)   _cols[j]=new TreeMap<Integer,Float>();
  if (v != null)   for (int i=0; i < rows; ++i)   for (int j=0; j < cols; ++j)   if (v.get(i,j) != 0f)   add(i,j,v.get(i,j));
}
