{
  gemv(_a.raw(),_w.raw(),_previous._a.raw(),_b.raw(),_dropout != null ? _dropout.bits() : null);
  for (int o=0; o < _a.size(); o++) {
    _a.set(o,1f - 2f / (1f + (float)Math.exp(2 * _a.get(o))));
  }
}
