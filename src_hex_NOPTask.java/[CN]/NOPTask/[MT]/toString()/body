{
  if (_missingKeysCount > 0)   return _missingKeysCount + " keys missing, Min = " + _minMissingKey+ ", Max = "+ _maxMissingKey+ ", byte xor = "+ _res;
  return "No keys were missing. Byte xor = " + _res;
}
