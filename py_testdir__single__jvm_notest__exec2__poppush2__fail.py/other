import unittest, random, sys, time
sys.path.extend(['.', '..', '../..', 'py'])
import h2o, h2o_browse as h2b, h2o_exec as h2e, h2o_import as h2i
initList = [('crunk', 'crunk=function(x){x+98};'), ('r.hex', 'r.hex=i.hex')]
phrases = ['crunk=function(x){x+98};', 'r.hex[,3]=4;']
if (__name__ == '__main__'):
    h2o.unit_main()
