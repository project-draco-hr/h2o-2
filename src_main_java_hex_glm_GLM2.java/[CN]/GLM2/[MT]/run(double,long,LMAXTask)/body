{
  String[] warns=new String[0];
  if (lmaxt != null)   lambda_max=lmaxt.lmax();
  double[] grad=lmaxt.gradient(0);
  System.out.println("lambda max = " + lambda_max + ",  + grad = "+ Arrays.toString(grad));
}
