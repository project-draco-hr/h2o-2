{
  if (DKV.get(dest()) == null)   return 0;
  GLMModel m=DKV.get(dest()).get();
  float progress=(float)m.iteration() / (float)max_iter;
  System.out.println("glm progress = " + progress);
  return progress;
}
