{
  boolean done=_iter == max_iter || _currentLambda <= lambda_min || (max_predictors != -1 && nzs(_beta) > max_predictors);
  if (!done)   nextLambda(nextLambdaValue(),new LambdaIteration());
 else   GLM2.this.complete();
}
