def rand_rowData():
    rowData = str(random.uniform(0, 7))
    for i in range(7):
        b = random.random()
        e = (random.random() * 20)
        s = random.randint(0, 1)
        f = ((b * (10 ** e)) * ((s and (-1)) or 1))
        rowData = ((rowData + ',') + str(f))
    return rowData
