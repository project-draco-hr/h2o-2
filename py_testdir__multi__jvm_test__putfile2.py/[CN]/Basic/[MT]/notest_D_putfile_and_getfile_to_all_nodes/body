def notest_D_putfile_and_getfile_to_all_nodes(self):
    cvsfile = file_to_put()
    for node in h2o.nodes:
        key = node.put_file(cvsfile)
        r = node.get_key(key)
        f = open(cvsfile)
        self.diff(r, f)
        f.close()
