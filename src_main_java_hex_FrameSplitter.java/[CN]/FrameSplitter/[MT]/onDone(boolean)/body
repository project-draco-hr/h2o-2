{
  dataset.unlock(jobKey);
  for (  Frame s : splits) {
    if (!exceptional) {
      s.update(jobKey);
      s.unlock(jobKey);
    }
 else {
      if (s != null)       s.delete(jobKey,0f);
    }
  }
}
