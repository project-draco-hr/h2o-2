{
  final Res res=new Res();
  Pixels task=new Pixels();
  task._arykey=va._key;
  task._width=width;
  task._height=height;
  task._cols=cols;
  task.invoke(va._key);
  res._pixels=task._pixels;
  UKV.put(dest,res);
}
