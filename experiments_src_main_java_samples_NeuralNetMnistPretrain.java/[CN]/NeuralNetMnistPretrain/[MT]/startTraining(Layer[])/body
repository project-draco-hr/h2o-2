{
  int pretrain_epochs=4;
  preTrain(ls,pretrain_epochs);
  int epochs=0;
  if (epochs > 0) {
    _trainer=new Trainer.Threaded(ls,epochs,self(),-1);
    _trainer.start();
    _trainer.join();
  }
}
