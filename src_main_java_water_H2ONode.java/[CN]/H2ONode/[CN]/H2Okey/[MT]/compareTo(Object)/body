{
  if (x == null)   return -1;
  H2Okey key=((H2ONode)x)._key;
  if (key == this)   return 0;
  int old=this.old_compare(key);
  int hack=this.hack_compare(key);
  if (old > 0)   old=1;
 else   if (old < 0)   old=-1;
  if (hack > 0)   hack=1;
 else   if (hack < 0)   hack=-1;
  return hack;
}
