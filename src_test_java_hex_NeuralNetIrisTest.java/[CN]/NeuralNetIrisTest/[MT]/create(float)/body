{
  Layer[] ls=new Layer[3];
  ls[0]=new FrameInput(_train,false);
  ls[0].init(null,4);
  ls[1]=new Layer.Tanh();
  ls[1]._rate=rate;
  ls[1].init(ls[0],7);
  ls[2]=new Softmax();
  ls[2]._rate=rate;
  ls[2].init(ls[1],3);
  for (int i=1; i < ls.length; i++)   ls[i].randomize();
  return ls;
}
