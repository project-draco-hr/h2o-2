def store_view(self, timeoutSecs=60, **kwargs):
    params_dict = {'view': 1024, }
    params_dict.update(kwargs)
    print '\nStoreView params list:', params_dict
    a = self.__do_json_request('StoreView.json', params=params_dict, timeout=timeoutSecs)
    return a
