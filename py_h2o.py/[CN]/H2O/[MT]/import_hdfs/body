def import_hdfs(self, path, timeoutSecs=180):
    a = self.__do_json_request(('2/ImportFiles2.json' if beta_features else 'ImportHdfs.json'), timeout=timeoutSecs, params={'path': path, })
    verboseprint('\nimport_hdfs result:', dump_json(a))
    return a
