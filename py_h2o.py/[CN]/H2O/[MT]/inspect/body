def inspect(self, key, offset=None, view=None, ignoreH2oError=False):
    a = self.__do_json_request('Inspect.json', params={'key': key, 'offset': offset, 'view': view, }, ignoreH2oError=ignoreH2oError)
    return a
