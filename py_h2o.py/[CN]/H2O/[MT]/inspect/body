def inspect(self, key, offset=None, view=None, ignoreH2oError=False):
    a = self.__check_request(requests.get(self.__url('Inspect.json'), params={'key': key, 'offset': offset, 'view': view, }), ignoreH2oError=ignoreH2oError)
    return a
