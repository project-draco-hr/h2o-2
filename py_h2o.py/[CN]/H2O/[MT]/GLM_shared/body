def GLM_shared(self, key, timeoutSecs=300, retryDelaySecs=0.5, initialDelaySecs=None, pollTimeoutSecs=30, parentName=None, **kwargs):
    browseAlso = kwargs.pop('browseAlso', False)
    params_dict = {'parallel': 1, 'family': 'binomial', 'key': key, 'y': 1, 'link': 'familyDefault', }
    params_dict.update(kwargs)
    print ('\n' + parentName), 'params list', params_dict
    a = self.__do_json_request(self.__url((parentName + '.json')), timeout=timeoutSecs, params=params_dict)
    verboseprint(parentName, dump_json(a))
    return a
