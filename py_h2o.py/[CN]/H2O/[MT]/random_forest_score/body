def random_forest_score(self, data_key, model_key, timeoutSecs=60, retryDelaySecs=0.5, initialDelaySecs=None, pollTimeoutSecs=180, **kwargs):
    rfView = random_forest_view(useRFScore=True, *args, **kwargs)
    return rfView
