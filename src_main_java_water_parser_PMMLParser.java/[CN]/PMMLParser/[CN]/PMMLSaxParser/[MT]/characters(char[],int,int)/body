{
  if (_needArrayContent && _expectedArraySize > 0) {
    String val=new String(ch,start,length);
    String[] values=val.split("\\s+");
    for (    String s : values)     if (s != null && !s.isEmpty())     _arrayContent.add(s);
  }
}
