def test_small_parse_overlap_same_dest_del_nopoll(self):
    csvPathname = 'poker/poker-hand-testing.data'
    for trials in range(230):
        src_key = csvPathname
        hex_key = (csvPathname + '.hex')
        parseResult = h2i.import_parse(bucket='smalldata', path=csvPathname, schema='put', src_key=src_key, hex_key=hex_key, timeoutSecs=180, noPoll=True, doSummary=False)
        h2o.nodes[0].remove_key(csvPathname)
