{
  Frame res=UKV.get(Key.make(target));
  return "Created interaction feature " + res.names()[source.numCols()] + " (order: "+ factors.length+ ") with "+ res.lastVec().domain().length+ " factor levels"+ " in"+ PrettyPrint.msecs(_time,true);
}
