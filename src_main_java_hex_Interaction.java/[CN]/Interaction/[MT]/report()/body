{
  Frame res=UKV.get(Key.make(target));
  return "Created interaction feature " + res.names()[0] + " (order: "+ factors.length+ ") with "+ res.lastVec().domain().length+ " factor levels"+ " in"+ PrettyPrint.msecs(_time,true);
}
