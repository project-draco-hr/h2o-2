{
  StringBuilder sb=new StringBuilder();
  sb.append("{");
  boolean first=true;
  for (int i=0; i < _val.length; i++) {
    if (!first)     sb.append(", ");
    sb.append(_val[i]);
    first=false;
  }
  sb.append("}");
  return sb.toString();
}
