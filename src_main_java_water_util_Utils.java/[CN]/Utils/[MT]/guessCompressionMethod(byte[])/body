{
  AutoBuffer ab=new AutoBuffer(bits);
  if (bits.length > ZipFile.LOCHDR && ab.get4(0) == ZipFile.LOCSIG)   return Compression.ZIP;
  if (bits.length > 2 && ab.get2(0) == GZIPInputStream.GZIP_MAGIC)   return Compression.GZIP;
  return Compression.NONE;
}
