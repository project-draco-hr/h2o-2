{
  fr.checkCompatible();
  if ((_noutputs=outputs) > 0)   _vid=fr.anyVec().group().reserveKeys(outputs);
  _fr=fr;
  _nodes=(1L << H2O.CLOUD.size()) - 1;
  setupLocal0();
  H2O.submitTask(this);
  return self();
}
