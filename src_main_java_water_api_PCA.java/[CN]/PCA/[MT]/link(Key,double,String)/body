{
  StringBuilder sb=new StringBuilder("<a href='PCA.query?");
  sb.append(KEY + "=" + k.toString());
  sb.append("&tolerance=" + tol);
  sb.append("'>" + content + "</a>");
  return sb.toString();
}
