{
synchronized (this) {
    int idx=_idx + 1;
    if (idx == 256)     idx=1;
    H2O cloud=CLOUD=new H2O(id,members,idx);
    CLOUDS[idx]=cloud;
  }
  Paxos.print("Announcing new Cloud Membership: ",members,"");
}
