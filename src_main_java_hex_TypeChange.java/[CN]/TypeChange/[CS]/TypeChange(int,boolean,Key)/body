{
  _col_index=column_index;
  _to_enum=to_enum;
  _ary=DKV.get(key).get();
  if (_to_enum) {
    CollectDomainVA cd=new CollectDomainVA(_col_index,new NonBlockingHashMapLong(),_ary);
    _cdVA=cd.invoke(key).domain();
  }
}
