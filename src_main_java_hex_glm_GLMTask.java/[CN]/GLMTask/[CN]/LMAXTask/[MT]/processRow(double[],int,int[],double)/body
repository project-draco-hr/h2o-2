{
  double w=(response - _ymu) * _gPrimeMu;
  for (int i=0; i < ncats; ++i)   _z[cats[i]]+=w;
  final int off=_catOffsets[_cats];
  ++_nobs;
  for (int i=0; i < nums.length; ++i)   _z[i + off]+=w * nums[i];
}
