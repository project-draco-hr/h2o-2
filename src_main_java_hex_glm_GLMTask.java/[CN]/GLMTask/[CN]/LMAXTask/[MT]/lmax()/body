{
  double res=Math.abs(_z[0]);
  for (int i=1; i < _z.length; ++i)   if (res < _z[i])   res=_z[i];
 else   if (res < -_z[i])   res=-_z[i];
  return _glm.variance(_ymu) * res / (_nobs * Math.max(_alpha,1e-3));
}
