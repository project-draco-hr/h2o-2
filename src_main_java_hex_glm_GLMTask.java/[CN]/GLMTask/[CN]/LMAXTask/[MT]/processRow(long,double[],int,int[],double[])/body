{
  double w=(responses[0] - _ymu) * _gPrimeMu;
  for (int i=0; i < ncats; ++i)   _z[cats[i]]+=w;
  final int numStart=_dinfo.numStart();
  ++_nobs;
  for (int i=0; i < nums.length; ++i)   _z[i + numStart]+=w * nums[i];
  _val.add(responses[0],_ymu);
}
