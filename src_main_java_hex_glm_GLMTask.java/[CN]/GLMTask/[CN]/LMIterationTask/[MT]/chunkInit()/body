{
  _gram=new Gram[n_folds];
  _xy=new double[n_folds][];
  for (int i=0; i < n_folds; ++i) {
    _gram[i]=new Gram(_dinfo.fullN(),_dinfo.largestCat(),_dinfo._nums,_dinfo._cats,true);
    _xy[i]=MemoryManager.malloc8d(_dinfo.fullN() + 1);
  }
}
