{
  _gram=new Gram(fullN(),diagN(),_nums,_cats,true);
  _xy=MemoryManager.malloc8d(fullN() + 1);
  int rank=0;
  if (_beta != null)   for (  double d : _beta)   if (d != 0)   ++rank;
  _val=new GLMValidation(null,_ymu,_glm,rank);
}
