{
  if (_ti[yi] > (_newThresholds.length >> 2)) {
    Arrays.sort(_newThresholds[yi]);
    for (int i=0; i < _ti[yi]; i+=4)     _newThresholds[yi][i >> 2]=_newThresholds[yi][i];
    _ti[yi]=_newThresholds.length >> 2;
  }
}
