import unittest
import random, sys, time
sys.path.extend(['.', '..', 'py'])
import h2o, h2o_cmd, h2o_browse as h2b, h2o_exec as h2e
exprList = ['Result<n> = Result0 * Result<n-1>', 'Result<n> = Result1 + Result<n-1>', 'Result<n> = Result2 / Result<n-1>', 'Result<n> = Result3 - Result<n-1>']
if (__name__ == '__main__'):
    h2o.unit_main()
