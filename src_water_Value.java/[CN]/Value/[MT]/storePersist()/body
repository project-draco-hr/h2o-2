{
  if (isPersisted())   return;
switch (_persist & BACKEND_MASK) {
case ICE:
    PersistIce.fileStore(this);
  break;
case HDFS:
PersistHdfs.fileStore(this);
break;
case NFS:
PersistNFS.fileStore(this);
break;
case S3:
PersistS3.fileStore(this);
break;
default :
throw H2O.unimpl();
}
}
