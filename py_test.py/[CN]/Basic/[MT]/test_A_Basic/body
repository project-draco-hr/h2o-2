def test_A_Basic(self):
    expectedSize = len(h2o.nodes)
    cloudSizes = [n.get_cloud()['cloud_size'] for n in h2o.nodes]
    for s in cloudSizes:
        self.assertEqual(s, expectedSize, ('Inconsistent cloud size. nodes report %s instead of %d' % (','.join(str(cloudSizes)), expectedSize)))
