{
  randomizeWeights();
  for (int i=0; i < parameters.hidden.length; ++i) {
    if (parameters.activation == DeepLearning.Activation.Rectifier || parameters.activation == DeepLearning.Activation.RectifierWithDropout || parameters.activation == DeepLearning.Activation.Maxout || parameters.activation == DeepLearning.Activation.MaxoutWithDropout) {
      Arrays.fill(biases[i],i == 0 ? 0.5 : 1.);
    }
 else     if (parameters.activation == DeepLearning.Activation.Tanh || parameters.activation == DeepLearning.Activation.TanhWithDropout) {
      Arrays.fill(biases[i],0.0);
    }
  }
  Arrays.fill(biases[biases.length - 1],0.0);
}
