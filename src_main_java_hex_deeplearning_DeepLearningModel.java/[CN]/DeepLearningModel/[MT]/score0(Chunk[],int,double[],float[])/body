{
  if (!get_params().autoencoder)   return super.score0(chks,row_in_chunk,tmp,preds);
 else {
    for (int i=0; i < _names.length - 1; i++)     tmp[i]=chks[i].at0(row_in_chunk);
    return score0(tmp,preds);
  }
}
