@classmethod
def setUpClass(cls):
    logging.basicConfig(filename='benchmark.log', level=logging.CRITICAL, format='%(asctime)s %(message)s')
    print 'Will build clouds with incrementing heap sizes and import folder/parse'
