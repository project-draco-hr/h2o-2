{
  if (!E.peek('='))   return null;
  AST ast2=ast;
  if ((ast instanceof ASTSlice))   ast2=((ASTSlice)ast)._ast;
  if (!(ast2 instanceof ASTKey))   return null;
  int x=E._x;
  AST eval=parseCXExpr(E);
  return new ASTAssign(ast,eval);
}
