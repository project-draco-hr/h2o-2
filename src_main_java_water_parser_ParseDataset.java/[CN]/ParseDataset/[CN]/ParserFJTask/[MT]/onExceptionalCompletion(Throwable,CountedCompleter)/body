{
  UKV.remove(job._progress);
  if (!(ex instanceof JobCancelledException))   job.cancel("Got Exception " + ex.getClass().getSimpleName() + ", with msg "+ ex.getMessage());
  return super.onExceptionalCompletion(ex,caller);
}
