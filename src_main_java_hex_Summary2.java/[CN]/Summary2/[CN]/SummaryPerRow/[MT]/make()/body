{
  Vec[] vecs=_fr.vecs();
  Summary2 sums[]=new Summary2[vecs.length];
  for (int i=0; i < vecs.length; i++)   sums[i]=new Summary2(vecs[i],_fr._names[i],vecs[i].min(),vecs[i].max());
  return new SummaryPerRow(_fr,sums);
}
