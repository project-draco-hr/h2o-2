{
  _summaries=new Summary2[cs.length];
  for (int i=0; i < cs.length; i++) {
    _summaries[i]=new Summary2(_fr.vecs()[i],_fr.names()[i],_mins[i],_maxs[i]);
    _summaries[i].add(cs[i]);
    _summaries[i].resample(cs[i],_resample_ix);
  }
}
