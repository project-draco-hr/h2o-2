{
  if (_id == null || d != 0) {
    if (_ds == null || _len >= _ds.length) {
      append2slowd();
      addNum(d);
      return;
    }
    if (_id != null)     _id[_len]=_len2;
    _ds[_len++]=d;
  }
  _len2++;
}
