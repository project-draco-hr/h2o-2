{
  if (_ls != null) {
    double ds[]=MemoryManager.malloc8d(_len);
    for (int j=0; j < _len; j++)     ds[j]=(isNA(j) || isEnum(j)) ? Double.NaN : _ls[j] * Math.pow(10,_xs[j]);
    _ds=ds;
    _ls=null;
    _xs=null;
  }
  if (Double.isNaN(_ds[i]))   _naCnt--;
  _ds[i]=d;
  if (Double.isNaN(d))   _naCnt++;
  return true;
}
