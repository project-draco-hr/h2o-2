{
  if (_id == null || l != 0) {
    if (_ls == null || _len == _ls.length) {
      append2slow();
      append2(l,x);
      return;
    }
    _ls[_len]=l;
    _xs[_len]=x;
    if (_id != null)     _id[_len]=_len2;
    _len++;
  }
  _len2++;
}
