{
  if (_ls == null || _len >= _ls.length)   append2slow();
  if (_len2 != _len) {
    if (x != 0) {
      long ls[]=MemoryManager.malloc8(_len2 + 1);
      for (int i=0; i < _len; i++)       ls[_xs[i]]=_ls[i];
      _ls=ls;
      _xs=MemoryManager.malloc4(_len2 + 1);
      _len=_len2;
    }
 else     if (l == 0) {
      _len2++;
      return;
    }
 else     x=_len2;
  }
  _ls[_len]=l;
  _xs[_len++]=x;
  _len2++;
}
