{
  if (_ls == null || _len >= _ls.length)   append2slow();
  if (_id == null) {
    _ls[_len]=l;
    _xs[_len]=x;
    _len++;
  }
 else   if (l != 0 || x != 0) {
    _ls[_len]=l;
    _id[_len]=_len2;
    _xs[_len]=x;
    _len++;
  }
  _len2++;
}
