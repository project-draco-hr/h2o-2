{
  if (_ls == null || _len >= _ls.length)   append2slow();
  if (_len2 != _len) {
    if (x != 0)     cancel_sparse();
 else     if (l == 0) {
      _len2++;
      return;
    }
 else     x=_len2;
  }
  _ls[_len]=l;
  _xs[_len++]=x;
  _len2++;
}
