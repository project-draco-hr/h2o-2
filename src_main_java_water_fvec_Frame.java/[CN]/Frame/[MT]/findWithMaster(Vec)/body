{
  HashSet<Vec> vecs=new HashSet<Vec>();
  for (int i=0; i < _vecs.length; i++) {
    Vec vm;
    if (((vm=_vecs[i].masterVec()) != null) && vm.equals(master))     vecs.add(_vecs[i]);
  }
  return vecs;
}
