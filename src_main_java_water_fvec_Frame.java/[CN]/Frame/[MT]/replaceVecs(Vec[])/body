{
  if (vecs.length != _vecs.length)   throw new IllegalArgumentException("Incompatible number of vecs");
  _vecs=vecs;
  _col0=_vecs[0];
  for (int i=0; i < _keys.length; ++i)   _keys[i]=vecs[i]._key;
}
