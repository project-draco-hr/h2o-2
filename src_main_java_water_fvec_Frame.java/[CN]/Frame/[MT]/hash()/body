{
  Vec[] vecs=vecs();
  byte[] _hash=new byte[8];
  for (int i=0; i < _names.length; ++i) {
    byte[] vec_hash=vecs[i].hash();
    for (int j=0; j < 8; j++) {
      _hash[j]^=vec_hash[j];
    }
  }
  return _hash;
}
