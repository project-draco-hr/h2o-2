{
  ValueArray va=ValueArray.value(DKV.get(k));
  Assert.assertEquals(len,va._numrows);
  Assert.assertEquals(expected[0].length,va._cols.length);
  for (int i=0; i < expected.length; ++i)   for (int j=0; j < va._cols.length; ++j) {
    if (Double.isNaN(expected[i][j]))     Assert.assertFalse(i + " -- " + j,!va.isNA(i,j));
 else     Assert.assertTrue(expected[i][j] + " -- " + va.datad(i,j),compareDoubles(expected[i][j],va.datad(i,j),0.001));
  }
  UKV.remove(k);
  UKV.remove(inputkey);
}
