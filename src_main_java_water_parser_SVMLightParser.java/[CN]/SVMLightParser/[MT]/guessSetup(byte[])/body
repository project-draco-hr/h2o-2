{
  InputStream is=new ByteArrayInputStream(bits);
  SVMLightParser p=new SVMLightParser(CustomParser.ParserSetup.makeSVMLightSetup(0,null,null));
  InspectDataOut dout=new InspectDataOut();
  try {
    p.streamParse(is,dout);
  }
 catch (  Exception e) {
    throw new RuntimeException(e);
  }
  return (dout._ncols > 1 && dout._nlines > 0 && (double)dout._invalidLines / dout._nlines < 0.1) ? CustomParser.ParserSetup.makeSVMLightSetup(dout._ncols,dout.data(),bits) : null;
}
