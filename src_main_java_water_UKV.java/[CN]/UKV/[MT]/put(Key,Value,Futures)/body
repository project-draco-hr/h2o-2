{
  Value res=DKV.put(key,val,fs);
  if (res != null && res.isArray()) {
    ValueArray ary=res.get();
    for (long i=0; i < ary.chunks(); i++)     DKV.remove(ary.getChunkKey(i),fs);
  }
  if (key._kb[0] == Key.KEY_OF_KEYS)   for (  Key k : key.flatten())   remove(k,fs);
}
