{
  _out.update(_job);
  Log.info("Created interaction feature " + _out.names()[_out.names().length - 1] + " (order: "+ _ci.factors.length+ ") with "+ _out.lastVec().domain().length+ " factor levels.");
  _out.unlock(_job);
}
