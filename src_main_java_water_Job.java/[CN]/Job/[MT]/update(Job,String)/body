{
  new TAtomic<List>(){
    @Override public List atomic(    List old){
      if (old != null && old._jobs != null)       for (      Job current : old._jobs)       if (current == job)       job.exception=exception;
      return old;
    }
  }
.invoke(LIST);
}
