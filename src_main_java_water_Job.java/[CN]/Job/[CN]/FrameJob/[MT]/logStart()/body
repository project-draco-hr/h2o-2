{
  super.logStart();
  if (source == null) {
    Log.info("    source: null");
  }
 else {
    Log.info("    source.numCols(): " + source.numCols());
    Log.info("    source.numRows(): " + source.numRows());
  }
}
