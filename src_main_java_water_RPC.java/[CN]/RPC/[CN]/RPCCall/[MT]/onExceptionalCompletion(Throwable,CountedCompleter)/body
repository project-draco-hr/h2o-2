{
  DTask dt=_dt;
  if (dt != null && !_computed) {
synchronized (this) {
      if (_computed)       return false;
      _computed=true;
    }
    dt.setException(ex);
  }
  onCompletion(caller);
  return false;
}
