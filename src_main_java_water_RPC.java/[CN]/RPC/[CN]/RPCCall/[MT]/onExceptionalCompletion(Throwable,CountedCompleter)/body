{
  _computed=true;
  boolean first=false;
  DTask dt=_dt;
  if (dt != null && dt._exception == null) {
synchronized (this) {
      if (dt._exception == null) {
        dt.setException(ex);
        first=true;
      }
    }
  }
  if (first)   onCompletion(caller);
  return false;
}
