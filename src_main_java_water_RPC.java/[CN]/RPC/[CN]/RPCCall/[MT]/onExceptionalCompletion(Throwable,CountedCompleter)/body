{
  if (!_computed) synchronized (this) {
    if (_computed)     return false;
    _computed=true;
  }
  DTask dt=_dt;
  if (dt != null)   dt.setException(ex);
  onCompletion(caller);
  return false;
}
