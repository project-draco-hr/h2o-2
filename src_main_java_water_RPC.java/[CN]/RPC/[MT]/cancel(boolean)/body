{
  boolean did=false;
synchronized (this) {
    if (!isCancelled()) {
      did=true;
      _target=null;
      UDPTimeOutThread.PENDING.remove(this);
      _target.taskRemove(_tasknum);
    }
    notifyAll();
  }
  return did;
}
