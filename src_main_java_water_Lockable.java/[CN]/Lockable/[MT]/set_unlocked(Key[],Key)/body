{
  if (lks[0] == job_key) {
    _lockers=null;
  }
 else   if (lks.length == 2) {
    _lockers=null;
  }
 else {
    _lockers=Arrays.copyOf(lks,lks.length - 1);
    int j=0;
    for (int i=1; i < lks.length; i++)     if (!job_key.equals(lks[i]))     _lockers[j++]=lks[i];
  }
}
