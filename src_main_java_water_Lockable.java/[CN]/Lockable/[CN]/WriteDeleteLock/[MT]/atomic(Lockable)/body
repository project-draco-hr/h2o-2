{
  _old=old;
  if (old != null && !old.is_unlocked()) {
    System.out.println("lock/del fails because " + _key + " locked by : "+ Arrays.toString(old._lockers));
    throw H2O.unimpl();
  }
  old.set_write_lock(null);
  return old;
}
