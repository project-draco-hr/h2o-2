{
  if (old == null)   return null;
  _old=old;
  if (!old.is_unlocked())   throw new IllegalArgumentException(old.errStr() + " " + _key+ " is in use.  Unable to delete now.");
  old.set_write_lock(null);
  return old;
}
