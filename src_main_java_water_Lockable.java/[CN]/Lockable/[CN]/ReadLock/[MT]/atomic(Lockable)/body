{
  if (old == null)   throw new IllegalArgumentException("Nothing to lock!");
  if (old.is_wlocked())   throw new IllegalArgumentException("Unable to read-lock " + _key + " by job "+ _job_key+ " because write_locked by job "+ _lockers[0]);
  old.set_read_lock(_job_key);
  return old;
}
