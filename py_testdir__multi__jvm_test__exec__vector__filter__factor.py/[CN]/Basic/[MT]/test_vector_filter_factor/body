def test_vector_filter_factor(self):
    importFolderPath = '/home/0xdiag/datasets/standard'
    if localhost:
        maxTrials = 200
        csvFilenameAll = [('covtype.data', 'cA', 5), ('covtype.data', 'cB', 5)]
    else:
        maxTrials = 20
        csvFilenameAll = [('covtype.data', 'cA', 5), ('covtype20x.data', 'cC', 50)]
    csvFilenameList = csvFilenameAll
    lenNodes = len(h2o.nodes)
    for (csvFilename, key2, timeoutSecs) in csvFilenameList:
        h2i.setupImportFolder(None, importFolderPath)
        parseResult = h2i.parseImportFolderFile(None, csvFilename, importFolderPath, key2=key2, timeoutSecs=2000)
        print csvFilename, 'parse time:', parseResult['response']['time']
        print "Parse result['destination_key']:", parseResult['destination_key']
        inspect = h2o_cmd.runInspect(None, parseResult['destination_key'])
        print ('\n' + csvFilename)
        h2e.exec_zero_list(zeroList)
        h2e.exec_expr_list_rand(lenNodes, exprList, key2, maxCol=53, maxRow=400000, maxTrials=maxTrials, timeoutSecs=timeoutSecs)
