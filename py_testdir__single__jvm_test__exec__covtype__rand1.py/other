import unittest
import random, sys, time
sys.path.extend(['.', '..', 'py'])
import h2o, h2o_cmd, h2o_browse as h2b, h2o_exec as h2e, h2o_hosts
zeroList = ['Result0 = 0', 'Result = 0']
exprList = ['Result<n> = slice(c.hex[<col1>],<row>)', 'Result<n> = colSwap(c.hex,<col1>,(c.hex[2]==0 ? 54321 : 54321))', 'Result<n> = c.hex[<col1>]', 'Result<n> = min(c.hex[<col1>])', 'Result<n> = max(c.hex[<col1>]) + Result.hex[0]', 'Result<n> = mean(c.hex[<col1>]) + Result.hex[0]', 'Result<n> = sum(c.hex[<col1>]) + Result.hex[0]', 'Result<n> = log(c.hex[<col1>]) + Result.hex[0]']
if (__name__ == '__main__'):
    h2o.unit_main()
