{
  for (; ; ) {
    Get get=get("Jobs.json",JobsRes.class);
    Assert.assertEquals(200,get._status);
    for (    Job job : ((JobsRes)get._res).jobs)     if (job.destination_key.equals(dst.toString()))     if (job.end_time.length() > 0)     return;
  }
}
