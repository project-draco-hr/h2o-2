{
  double bs=Double.MAX_VALUE;
  int idx=-1;
  int ncols=_bins.length;
  for (int i=0; i < ncols; i++) {
    if (_bins[i].length == 1)     continue;
    double s=score(i);
    if (s < bs) {
      bs=s;
      idx=i;
    }
  }
  return idx;
}
