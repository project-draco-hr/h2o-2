{
  float sum=0;
  float[] p=super.score0(data,preds);
  if (nclasses() > 1) {
    for (int k=0; k < p.length; k++)     sum+=(p[k]=(float)Math.exp(p[k]));
    div(p,sum);
  }
 else {
  }
  return p;
}
