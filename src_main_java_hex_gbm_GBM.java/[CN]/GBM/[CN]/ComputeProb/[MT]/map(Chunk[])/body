{
  Chunk ys=chk_resp(chks);
  double ds[]=new double[_nclass];
  for (int row=0; row < ys._len; row++) {
    double sum=score0(chks,ds,row);
    for (int k=0; k < _nclass; k++)     chk_work(chks,k).set0(row,(float)(ds[k] / sum));
  }
}
