{
  double bs=Double.MAX_VALUE;
  int idx=-1;
  for (int i=0; i < hs.length; i++) {
    if (hs[i] == null || hs[i]._bins.length == 1)     continue;
    double s=hs[i].score();
    if (s < bs) {
      bs=s;
      idx=i;
    }
  }
  return idx;
}
