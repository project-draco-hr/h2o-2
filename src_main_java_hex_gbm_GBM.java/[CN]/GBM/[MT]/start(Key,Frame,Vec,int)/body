{
  final GBM job=new GBM(dest,fr);
  H2O.submitTask(job.start(new H2OCountedCompleter(){
    @Override public void compute2(){
      job.run(fr,vresponse,maxDepth);
      tryComplete();
    }
  }
));
  return job;
}
