{
  if (importance)   _improvPerVar=new float[initialModel.nfeatures()];
  if (family == Family.bernoulli && _nclass != 2)   throw new IllegalArgumentException("Bernoulli requires the response to be a 2-class categorical");
}
