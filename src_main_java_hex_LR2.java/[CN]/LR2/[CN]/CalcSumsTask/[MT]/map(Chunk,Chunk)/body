{
  for (int i=0; i < xs._len; i++) {
    double X=xs.getd(i);
    double Y=ys.getd(i);
    if (!Double.isNaN(X) && !Double.isNaN(Y)) {
      _sumX+=X;
      _sumY+=Y;
      _sumX2+=X * X;
      _n++;
    }
  }
}
