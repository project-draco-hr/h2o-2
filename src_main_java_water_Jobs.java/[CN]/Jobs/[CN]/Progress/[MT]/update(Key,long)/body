{
  new TAtomic<Progress>(){
    @Override public Progress atomic(    Progress old){
      old._value+=delta;
      return old;
    }
    @Override public Progress alloc(){
      return new Progress();
    }
  }
.fork(key);
}
