{
  for (  Constructor c : cl.getConstructors()) {
    Class[] ps=c.getParameterTypes();
    if (ps.length == 1 && Request2.class.isAssignableFrom(ps[0]))     return c;
  }
  throw new Exception("Class " + cl.getName() + " must have an empty constructor");
}
