{
  value=value.trim();
  try {
    return Integer.valueOf(value);
  }
 catch (  NumberFormatException e) {
    Frame fr=fr();
    for (int i=0; i < fr.numCols(); ++i)     if (fr._names[i].equals(value))     return i;
  }
  return -1;
}
