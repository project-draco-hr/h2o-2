{
  Value v=UKV.getValue(Key.make(input));
  if (v == null)   throw new IllegalArgumentException("Key " + input + " was not found");
  try {
    return v.get(new RFModel());
  }
 catch (  Exception e) {
    throw new IllegalArgumentException("Key " + input + " is not a model key");
  }
}
