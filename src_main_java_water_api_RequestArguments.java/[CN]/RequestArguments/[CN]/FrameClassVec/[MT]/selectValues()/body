{
  ArrayList<String> as=new ArrayList();
  Frame fr=fr();
  if (fr != null) {
    Vec vecs[]=fr.vecs();
    for (int i=0; i < vecs.length; i++)     if (filter(vecs[i]))     as.add(fr()._names[i]);
  }
  return as.toArray(new String[as.size()]);
}
