{
  if (_key.value() == null || _classCol.value() == null || _key.value()._cols[_classCol.value()]._domain == null)   return super.selectValues();
  return new String[]{CaseMode.eq.toString(),CaseMode.neq.toString()};
}
