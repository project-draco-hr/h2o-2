{
  ValueArray va=_key.value();
  Integer cc=_classCol.value();
  if (va == null || cc == null || va._cols[cc]._domain == null)   return super.selectValues();
  return new String[]{CaseMode.eq.toString(),CaseMode.neq.toString()};
}
