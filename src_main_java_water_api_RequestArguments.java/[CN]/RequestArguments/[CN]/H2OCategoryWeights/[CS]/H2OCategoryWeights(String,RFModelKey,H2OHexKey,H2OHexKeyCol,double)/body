{
  super(name,false);
  _modelKey=modelKey;
  _key=key;
  _classCol=classCol;
  _defaultValue=defaultValue;
  if (modelKey != null)   addPrerequisite(modelKey);
  addPrerequisite(key);
  addPrerequisite(classCol);
}
