{
  Integer N=super.parse(input);
  RFModel model=_modelKey.value();
  if (N > model.treeCount())   throw new IllegalArgumentException("Value " + N + " is higher than number of trees provided by the random forest model ("+ model.treeCount()+ ")!");
  return N;
}
