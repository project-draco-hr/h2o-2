{
  ValueArray va=_key.value();
  int colIdx=vaColumnNameToIndex(va,input);
  if (colIdx == -1)   throw new H2OIllegalArgumentException(this,input + " not a name of column, or a column index");
  return colIdx;
}
