{
  if (ca._min == ca._max) {
    if (_constantColumns.get() == null)     _constantColumns.set(new TreeSet<String>());
    _constantColumns.get().add(Objects.firstNonNull(ca._name,String.valueOf(i)));
    return true;
  }
  return super.shouldIgnore(i,ca);
}
