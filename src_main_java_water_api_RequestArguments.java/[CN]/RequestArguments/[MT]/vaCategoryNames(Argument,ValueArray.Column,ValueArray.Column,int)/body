{
  String[] result=ConfusionTask.domain(modelCol,dataCol);
  if (result.length > maxClasses)   throw new H2OIllegalArgumentException(caller,"The column has more than " + maxClasses + " values. Are you sure you have that many classes?");
  return result;
}
