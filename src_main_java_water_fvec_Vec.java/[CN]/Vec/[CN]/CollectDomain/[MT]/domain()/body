{
  int cnt=0;
  for (int i=0; i < _dom.length; i++)   if (_dom[i] > 0)   cnt++;
  int[] dom=new int[cnt];
  cnt=0;
  for (int i=0; i < _dom.length; i++)   if (_dom[i] > 0)   dom[cnt++]=i + _ymin;
  return dom;
}
