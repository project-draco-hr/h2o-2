{
  long min=(long)min(), max=(long)max();
  if (min < 0 || max > 100000L)   throw H2O.unimpl();
  String domain[]=new String[(int)max + 1];
  for (int i=0; i < (int)max + 1; i++)   domain[i]=Integer.toString(i);
  return domain;
}
