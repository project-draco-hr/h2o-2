{
  if (_dtype == t)   return;
  if (_dtype == Vec.DType.U) {
    _dtype=t;
    return;
  }
  System.out.println("mixing writes of type " + t + " into a Vec of type "+ _dtype);
  throw H2O.unimpl();
}
