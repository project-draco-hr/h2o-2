{
  String s="[" + length() + (Double.isNaN(_min) ? "" : "," + _min + "/"+ _max+ ", "+ PrettyPrint.bytes(byteSize())+ ", {");
  int nc=nChunks();
  for (int i=0; i < nc; i++)   s+=(Chunk)(DKV.get(chunkKey(i)).get()) + ",";
  return s + "}]";
}
