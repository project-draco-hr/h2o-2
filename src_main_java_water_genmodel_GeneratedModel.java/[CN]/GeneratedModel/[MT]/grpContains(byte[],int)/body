{
  int max_idx=(gcmp.length << 3) - 1;
  if (idx < 0 || idx > max_idx)   throw new IndexOutOfBoundsException("Must have 0 <= idx <= " + max_idx + ": "+ idx);
  return (gcmp[idx >> 3] & ((byte)1 << idx)) != 0;
}
