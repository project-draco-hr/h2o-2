{
  long processed=_training.processed();
  float m=momentum(processed);
  float r=rate(processed) * (1 - m);
  float[] v=target();
  for (int u=0; u < _a.length; u++) {
    float e=v[u] - _a[u];
    float g=e * (1 - _a[u]) * _a[u];
    bprop(u,g,r,m);
  }
}
