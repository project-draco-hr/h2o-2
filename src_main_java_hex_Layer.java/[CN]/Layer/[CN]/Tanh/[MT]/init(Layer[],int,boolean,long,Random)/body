{
  super.init(ls,index,weights,step,rand);
  if (weights) {
    final float range=(float)Math.sqrt(6. / (_previous.units + units));
    for (int i=0; i < _w.length; i++)     _w[i]=rand(rand,-range,range);
  }
}
