{
  for (int o=0; o < _a.length; o++) {
    _a[o]=Float.MIN_VALUE;
    float cur=0.0f;
    for (int i=0; i < _in._a.length; i++)     cur=_w[o * _in._a.length + i] * _in._a[i] * (rand.nextFloat() < prob ? 1 : 0)* scale;
    _a[o]=java.lang.Math.max(cur,_a[o]);
    _a[o]+=_b[o];
  }
}
