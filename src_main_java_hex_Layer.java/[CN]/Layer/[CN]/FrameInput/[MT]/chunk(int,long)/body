{
  if (_caches == null)   _caches=new Chunk[_frame.vecs().length];
  Chunk c=_caches[i];
  if (c != null && c._start <= n && n < c._start + c._len)   return c;
  return _caches[i]=_frame.vecs()[i].chunk(n);
}
