{
  for (int i=0; i < _w.length; i++)   adjust(i,_w,_wPrev,_wInit,_wMult);
  for (int i=0; i < _b.length; i++)   adjust(i,_b,_bPrev,_bInit,_bMult);
  _r=_rate / (1 + _rateAnnealing * n);
  _m=_momentum * (n + 1) / ((n + 1) + _momentumAnnealing);
}
