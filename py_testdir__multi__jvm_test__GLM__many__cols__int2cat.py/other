import unittest
import random, sys, time, os
sys.path.extend(['.', '..', 'py'])
import h2o, h2o_cmd, h2o_hosts, h2o_glm
import h2o_browse as h2b, h2o_import as h2i, h2o_exec as h2e
paramDict = {'family': ['binomial'], 'lambda': [1e-05], 'max_iter': [50], 'weight': [1.0], 'thresholds': [0.5], 'n_folds': [2], 'beta_eps': [0.0001], }
if (__name__ == '__main__'):
    h2o.unit_main()
