{
  Set<String> set=new HashSet<String>();
  if (Boot._init.fromJar()) {
    if (new File("target/h2o.jar").exists())     set.add("target/h2o.jar");
 else     set.add("h2o.jar");
  }
 else {
    set.add("target");
    set.add("lib");
  }
  return set;
}
