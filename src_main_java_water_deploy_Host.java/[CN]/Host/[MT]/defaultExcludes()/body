{
  ArrayList<String> l=new ArrayList<String>();
  if (!Boot._init.fromJar()) {
    l.add("target/*.jar");
    l.add("lib/javassist");
    l.add("**/*-sources.jar");
  }
  return l.toArray(new String[0]);
}
