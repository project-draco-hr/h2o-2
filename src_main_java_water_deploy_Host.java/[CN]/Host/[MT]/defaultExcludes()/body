{
  Set<String> set=new HashSet<String>();
  if (!Boot._init.fromJar()) {
    set.add("target/*.jar");
    set.add("target/javadoc/**");
    set.add("lib/javassist");
    set.add("**/*-sources.jar");
  }
  return set;
}
