def runPCA(node=None, parseResult=None, timeoutSecs=600, **kwargs):
    if (not parseResult):
        raise Exception('No parsed key for PCA specified')
    if (not node):
        node = h2o.nodes[0]
    data_key = parseResult['destination_key']
    return node.pca(data_key=data_key, **kwargs)
