def runStore2HDFS(node=None, key=None, timeoutSecs=5, **kwargs):
    if (not key):
        raise Exception('No key for Inspect specified')
    if (not node):
        node = h2o.nodes[0]
    return node.store2HDFS(key, **kwargs)
