@classmethod
def setUpClass(cls):
    global localhost
    localhost = h2o.decide_if_localhost()
    if localhost:
        h2o.build_cloud(1, use_hdfs=True, hdfs_name_node='192.168.1.176', hdfs_version='cdh3', java_heap_GB=28)
    else:
        h2o_hosts.build_cloud_with_hosts(1)
