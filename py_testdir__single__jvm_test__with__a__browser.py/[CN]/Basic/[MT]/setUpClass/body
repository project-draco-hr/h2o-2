@classmethod
def setUpClass(cls):
    global localhost
    localhost = h2o.decide_if_localhost()
    h2o.beta_features = True
    if localhost:
        h2o.build_cloud(1, use_hdfs=True, java_heap_GB=20, base_port=54321, java_extra_args='-XX:+PrintGCDetails')
    else:
        h2o_hosts.build_cloud_with_hosts(1, base_port=54321, java_heap_GB=20, java_extra_args='-XX:+PrintGCDetails')
