{
  _bits=new AutoBuffer(DKV.get(key).memOrLoad());
  final int nrows=_ary.rpc(ValueArray.getChunkIndex(key));
  final Row r=new Row(this);
  _res=newRes();
  for (_rid=0; _rid < nrows; ++_rid)   map(r,_res);
}
