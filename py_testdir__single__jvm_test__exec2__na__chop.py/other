import unittest, random, sys, time
sys.path.extend(['.', '..', '../..', 'py'])
import h2o, h2o_browse as h2b, h2o_exec as h2e, h2o_import as h2i, h2o_cmd
initList = [('r.hex', 'r.hex=i.hex')]
exprList = ['s.hex = r.hex[!is.na(r.hex[,<col1>]),]']
if (__name__ == '__main__'):
    h2o.unit_main()
