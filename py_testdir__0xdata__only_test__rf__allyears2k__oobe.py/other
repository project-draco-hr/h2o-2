import unittest, random, sys, time
sys.path.extend(['.', '..', 'py'])
import h2o, h2o_cmd, h2o_rf as h2f, h2o_hosts, h2o_import as h2i
paramDict = {'response_variable': 30, 'ntree': 50, 'model_key': 'model_keyA', 'out_of_bag_error_estimate': 1, }
if (__name__ == '__main__'):
    h2o.unit_main()
