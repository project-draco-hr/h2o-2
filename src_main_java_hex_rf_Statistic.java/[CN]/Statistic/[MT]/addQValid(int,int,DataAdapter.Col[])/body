{
  for (  int f : _features)   if (f != -1) {
    short[] bins=cs[f]._binned;
    int val=bins == null ? (0xFF & cs[f]._rawB[ridx]) : bins[ridx];
    if (bins == null || val != DataAdapter.BAD)     _columnDists[f][val][cls]++;
  }
}
