{
  for (  int f : _features)   if (f != -1) {
    short[] bins=cs[f]._binned;
    int val=bins == null ? cs[f]._rawB[ridx] : bins[ridx];
    if (val != DataAdapter.BAD)     _columnDists[f][val][cls]++;
  }
}
