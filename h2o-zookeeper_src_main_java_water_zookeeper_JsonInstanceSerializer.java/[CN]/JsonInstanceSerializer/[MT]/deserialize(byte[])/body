{
  ServiceInstance rawServiceInstance=gson.fromJson(new InputStreamReader(new ByteArrayInputStream(bytes)),token.getType());
  payloadClass.cast(rawServiceInstance.getPayload());
  return (ServiceInstance<T>)rawServiceInstance;
}
