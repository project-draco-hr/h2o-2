{
  javax.swing.SwingUtilities.invokeLater(new Runnable(){
    public void run(){
      try {
        URI uri=new URI(s);
        Desktop.getDesktop().browse(uri);
      }
 catch (      Exception e) {
      }
    }
  }
);
}
