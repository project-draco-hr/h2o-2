{
  File file=TestUtil.find_test_file("../covtype.csv");
  Key fkey=NFSFileVec.make(file);
  Key okey=Key.make("covtype.hex");
  ParseDataset2.parse(okey,new Key[]{fkey});
  for (int i=0; i < 9; i++)   UKV.remove(fkey.toString() + "C" + i);
  UKV.remove(fkey);
}
