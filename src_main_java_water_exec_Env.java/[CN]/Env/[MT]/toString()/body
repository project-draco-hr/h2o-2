{
  String s="{";
  for (int i=0; i < _sp; i++) {
    if (_fr[i] != null)     s+=_fr[i].numCols() + "x" + _fr[i].numRows();
 else     if (_fun[i] != null)     s+=_fun[i];
 else     s+=_d[i];
    s+=',';
  }
  return s + "}";
}
