{
  double coef=jcoefs.get("0." + category).getAsDouble();
  double predict=1.0 / (1.0 + Math.exp(-(coef * 1.0 + icept)));
  System.out.println("expected = " + expected + ", got "+ predict);
  assertEquals(expected,predict,0.001);
}
