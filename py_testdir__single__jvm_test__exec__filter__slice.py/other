import unittest, random, sys, time
sys.path.extend(['.', '..', 'py'])
import h2o, h2o_cmd, h2o_hosts, h2o_browse as h2b, h2o_import as h2i, h2o_exec as h2e
exprList = ['rTrain=randomFilter(<keyX>,522,12345)', 'r2=slice(rTrain,1,100)']
if (__name__ == '__main__'):
    h2o.unit_main()
