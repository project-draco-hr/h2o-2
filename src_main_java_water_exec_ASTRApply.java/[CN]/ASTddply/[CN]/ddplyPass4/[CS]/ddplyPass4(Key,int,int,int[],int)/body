{
  _nlocals=nlocals;
  _envkey=envkey;
  _numgrps=numgrps;
  Key keys[]=Vec.VectorGroup.VG_LEN1.addVecs(ccols + ncols);
  _avs=new AppendableVec[ccols + ncols];
  for (int i=0; i < _avs.length; i++)   _avs[i]=new AppendableVec(keys[i]);
}
