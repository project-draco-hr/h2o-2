{
  Lloyds task=(Lloyds)rt;
  if (_sums == null) {
    _sums=task._sums;
    _counts=task._counts;
    _error=task._error;
  }
 else {
    Utils.add(_sums,task._sums);
    Utils.add(_counts,task._counts);
    _error+=task._error;
  }
}
