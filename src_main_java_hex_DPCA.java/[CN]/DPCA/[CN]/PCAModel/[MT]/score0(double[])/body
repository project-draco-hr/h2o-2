{
  double[] mapped=new double[_num_pc];
  for (int j=0; j < _num_pc; j++) {
    for (int i=0; i < data.length; i++) {
      if (Double.isNaN(data[i])) {
        mapped[j]=Double.NaN;
        break;
      }
 else       mapped[j]+=data[i] * _eigVec[i][j];
    }
  }
  return mapped[0];
}
